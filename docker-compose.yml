services:
  health-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - NOTION_TOKEN
      - DB_SEMAINES_ID
      - DB_JOURS_ID
      - DB_WORKOUT_ID
      - API_KEY
    restart: unless-stopped
